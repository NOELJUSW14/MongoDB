
  <h1 class="card center-align">Latest Sunday Funnies</h1>
  <div class="row">
    <h3 class"card center-align">Click "Add" to save</h3> 
  </div>
  <div class="row">
    <div class="col s8 left-align">
      <div class="row" id="comics">
        {{!-- // $("#comics").append("<p comic-id='" + data[i]._id + "'>" + data[i].title + "by" + data[i].author +"<br />" + data[i].link + "</p>"); --}}

        {{!-- {{#if  Comics}}
        {{#each Comics}} --}}
{{Comics}}
{{!-- <p>{{{Comics.title}}}</p>
{{/each}} --}}
        {{!-- {{#each Comics.comment}}
        <div class="card">
            <p id="nameComment">{{name}}</p>
            <p>{{body}}</p>
        </div>
        {{/each}}
        {{else}}
        <p>No Comments</p>
        {{/if}} --}}

      </div>
    </div>
  </div>
  <div class="form-group">
    <h2>Add a Comic</h2>
    <button type="submit">Add Comic</button>
  </div>